apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: minio-dashboard
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`minio.chenchik.me`)
      services:
        - name: minio
          port: 9001
